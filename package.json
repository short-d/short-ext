{
  "name": "Short",
  "version": "0.0.4",
  "description": "Visit short links with less typing",
  "repository": {
    "type": "git",
    "url": "https://github.com/byliuyang/short-ext.git"
  },
  "scripts": {
    "dev": "yarn run cp && tsc --watch",
    "build": "yarn run cp && tsc",
    "cp": "cp -r icons extension/ && cp manifest.json extension/"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.87",
    "typescript": "^3.5.3"
  }
}
